fn main() {
    println!("Hello, world!");
}

// 1. run (cargo init)
// 2. run the program using (cargo run)
// 3. run (cargon build) this will make main.rs => to executable format
// 4. Target folder is not required in the github repository, use .gitignore with target